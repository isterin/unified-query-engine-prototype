services:
  postgres:
    image: postgres:16-alpine
    container_name: uqe-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: warehouse
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: uqe-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # Console
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  # One-time setup container to create MinIO buckets
  minio-setup:
    image: minio/mc:latest
    container_name: uqe-minio-setup
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 minioadmin minioadmin;
      mc mb myminio/analytics --ignore-existing;
      mc mb myminio/inventory --ignore-existing;
      mc mb myminio/delta --ignore-existing;
      mc anonymous set public myminio/analytics;
      mc anonymous set public myminio/inventory;
      mc anonymous set public myminio/delta;
      echo 'MinIO buckets created: analytics, inventory, delta';
      exit 0;
      "

volumes:
  postgres_data:
  minio_data:
